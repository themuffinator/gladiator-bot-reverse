// Autogenerated by tests/engine_parity/run_engine_parity.py
// Values wrapped in braces are substituted at runtime.
set botlib "{botlib}"
set logfile 2
set maxclients {maxclients}
set timelimit {timelimit}
set fraglimit {fraglimit}
set dmflags {dmflags}
map {map_name}
{bot_commands}
// Optional demo capture. The command is ignored if the server does not support recording.
{record_command}
// The harness will wait for the requested match duration and then send a quit command.
// Additional scripted actions can be appended below.
