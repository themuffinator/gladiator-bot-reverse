# HLIL Movement Helper Notes vs. Quake III Botlib

## Overview
The Gladiator HLIL exposes the same high-level movement flow that Quake III Arena implements in `be_ai_move.c` and `be_ai_goal.c`. The snippets below capture the state machines and special cases that must be recreated when we lift the decompiled helpers into C. The Quake III sources are used as a reference for naming and structure; the HLIL confirms which branches are still present in Gladiator and where behaviour diverges.

## Core Move State Tracking
* Quake III stores per-bot movement state in `bot_movestate_t`, including fields for positional history, reachability bookkeeping, and bitflags such as `MFL_SWIMMING`, `MFL_AGAINSTLADDER`, and grapple state. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L56-L119】
* The Gladiator HLIL clears the same flag mask (`&= 0xfffffff3`) and immediately repopulates bits by calling helpers equivalent to `BotSwimInWater`, `BotOnLadder`, and `BotJumpOrFall`, matching the Quake logic that rebuilds `ms->moveflags` each frame. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40697-L40720】
* HLIL offsets `0x13` (last reachability), `0x18` (move flag byte), and `0x1c` (reachability timeout) mirror Quake’s `lastreachnum`, `moveflags`, and `reachability_time`. Updating those slots happens just before or after dispatching per-travel-type handlers, preserving the same temporal ordering. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40697-L40740】

## Travel-Type Dispatch Tables
* Quake III maps reachability travel types to specialised finish routines inside `BotMoveToGoal`, logging when an unrecognised type appears. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L3213-L3315】
* Gladiator’s `sub_100343a0` uses the same switch, falling back to `data_10063fe8("(last) travel type %d not implemented yet")` for unsupported branches, and copies a 0x30-byte movement result structure from each helper before returning. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40712-L40790】
* `sub_10034170` computes a per-travel-type timeout (5s for standard walking, 6s for ladders and bobbing, 10s for grapple) identical to the Quake heuristics that seed `ms->reachability_time`. Unsupported types emit a diagnostic before defaulting to 8 seconds. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40563-L40582】

## Grapple / Hook State Machine
* Quake III caches cvars such as `laserhook`, loads the grapple model on demand, and toggles the `hookon` / `hookoff` commands based on reachability distance inside `BotTravel_Grapple`. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L2534-L2737】
* Gladiator mirrors that flow: `sub_100338a0` grabs the `laserhook` libvar and precaches `models/weapons/grapple/hook/tris.md2` the first time it detects an enabled hook. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40193-L40257】
* The bridge leaves grapples disabled unless the `usehook` libvar is explicitly defined, so parity builds must opt in before any hook-specific paths become active.
* The grapple handler sets bit `0x40` in the move flags when `hookon` is issued, caches the reel distance in offsets `0x1a`/`0x1b`, and forcibly toggles `hookoff` when the slack exceeds one unit or the bot drifts more than 48 units from the last recorded distance—behaviour that aligns with Quake’s tension checks. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40385-L40460】
* Timeouts use `j_sub_1000e120()` (the HLIL wrapper for `AAS_Time()`) to gate reactivation, matching the Quake guard that waits before firing the hook again. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40393-L40445】

## Obstacle and Platform Handling
* Quake’s ground branch in `BotMoveToGoal` inspects `modeltypes` to detect when bots land atop elevators, bobbing platforms, doors, or static brushes, then rewires reachabilities or reports blockage. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L3088-L3180】
* Gladiator’s `sub_100160e0` walks the entity list, matching `classname` strings such as `func_plat`, `func_bobbing`, and related movers, extracting lip, height, and speed fields to populate the same lookup table. That preparation is required so the movement loop can resolve accidental landings just like Quake III. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L18002-L18267】
* Later in the frame, the HLIL copies the bot’s origin into `0x14`/`0x16` after finishing travel dispatch, mirroring the Quake behaviour of caching the last grounded location for platform correction and stuck detection. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40725-L40739】

## Goal Selection and Per-Frame Sequencing
* Quake III maintains a goal stack (`bot_goalstate_t`) and computes long-term and nearby goals each frame via `BotChooseLTGItem` / `BotChooseNBGItem`, combining fuzzy item weights with AAS travel times. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_goal.c†L1241-L1519】
* The Gladiator HLIL shows the movement helper taking both the active goal blob (`arg3`) and the current move state (`arg2`), exiting early when `arg2[0x10]` (current reachability) matches the goal’s area—exactly where Quake stops to push or pop goals. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40791-L40806】
* After pathing, Gladiator calls a routine equivalent to `BotGetReachabilityToGoal`, storing new avoid lists in offsets `0x1d`–`0x1f`, and clears the hook state when the path changes—matching Quake’s per-frame order of operations. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40810-L40820】

## Reconstructed Wiring Round (2026-05-24)
* `BotInitMoveState` now mirrors Q3's frame refresh instead of resetting the whole move state: the retail routine copies the current input fields and toggles only `MFL_ONGROUND`, `MFL_TELEPORTED`, `MFL_WATERJUMP`, `MFL_WALK`, and `MFL_GRAPPLEPULL`. Preserving the current reachability fields is required for the HLIL timeout and last-reach checks that follow. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L188-L213】【F:src/botlib/ai_move/bot_move.c†L2153-L2199】
* The reconstructed `BotGetReachabilityToGoal` follows Q3's route-selection contract: promote `TFL_DONOTENTER` when either endpoint is a do-not-enter area, enumerate outgoing reachabilities, skip the previous area when the goal did not change, validate travel and area flags, reject exhausted avoid-reach entries, reject avoid spots with `MOVERESULT_BLOCKEDBYAVOIDSPOT`, then choose the lowest `AAS_AreaTravelTimeToGoalArea + reach.traveltime` candidate. A local Dijkstra fallback is retained for tests and partially reconstructed AAS worlds that do not have reverse route caches yet. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L744-L815】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40816-L40820】【F:src/botlib/ai_move/bot_move.c†L1556-L1669】
* Active reach dispatch in Gladiator's `sub_100343a0` maps type `2` to walk (`j_sub_10031e50`), `3` to crouch (`j_sub_100320c0`), `4` to barrier jump (`j_sub_10032190`), `5` to jump (`j_sub_10032ae0`), `6` to ladder (`j_sub_10032fc0`), `7` to walk-off-ledge (`j_sub_100327f0`), `8` to swim (`j_sub_100323e0`), `9` to water jump (`j_sub_100324c0`), `10` to teleport (`j_sub_100330e0`), `11` to elevator (`j_sub_10033210`), `12` to rocket jump (`j_sub_10033ec0`), `13` to the unsupported diagnostic, and `14` to grapple (`j_sub_10033b00`). The C dispatcher now keeps those branch identities while routing unsupported successor types through the original warning behavior. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40897-L40955】【F:src/botlib/ai_move/bot_move.c†L2247-L2357】
* `BotAddAvoidSpot` is now wired as a first-class movement export. Q3 appends avoid spots until `MAX_AVOIDSPOTS` and uses `AVOID_CLEAR` only to reset the list; the bridge and interface export table now expose that exact path for game-side callers. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L719-L737】【F:dev_tools/Quake-III-Arena-master/code/botlib/be_interface.c†L813-L826】【F:src/botlib/ai_move/bot_move.c†L3736-L3767】
* `BotMoveInDirection` now calls into EA movement/action submission instead of merely normalizing output state. It preserves the walk/swim split from Q3 and drives jump, crouch, and grapple request bits through the same input collection layer used by travel helpers. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1231-L1246】【F:src/botlib/ai_move/bot_move.c†L3361-L3381】

## Direct Movement Barrier Reconstruction (2026-05-26)
* Q3's exported `BotMoveInDirection` only dispatches to swim or walk helpers; `BotWalkInDirection` probes `BotCheckBarrierJump` before normal walking, clears `MFL_BARRIERJUMP` after a failed ground probe, and only continues airborne barrier movement when vertical velocity falls below 50. The reconstructed C now follows that helper split while retaining the existing EA input submission path for direct movement. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1049-L1246】【F:src/botlib/ai_move/bot_move.c†L3274-L3487】
* Gladiator's libvar wiring differs from stock Q3 on barrier height: the HLIL records `sv_step` with fallback `18` and `sv_maxbarrier` with fallback `50`. The bridge now caches the step key as `sv_step`, and the direct barrier probe reads both values through `Bridge_MaxStep` / `Bridge_MaxBarrier`. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L43421-L43422】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L75524-L75533】【F:src/q2bridge/bridge_config.c†L107-L108】
* The reconstructed `BotMove_CheckBarrierJump` performs the retail vertical-forward-down trace sequence with normal presence bounds, sets `EA_Jump`, `EA_Move`, `MFL_BARRIERJUMP`, and `jumpreach` on success, and focused coverage pins the trace order, libvar names, and late-airborne continuation. 【F:src/botlib/ai_move/bot_move.c†L3274-L3390】【F:tests/ai/test_bot_move.c†L479-L588】
* `BotMove_GapDistance` now mirrors Q3's direct-walk gap scan: trace down with crouch bounds to smooth stairs, sample forward in eight-unit increments, compare drops against `sv_step`, and skip jump promotion when the drop lands in water. `BotMove_WalkInDirection` uses that probe to promote `MOVE_WALK` into `MOVE_JUMP` before EA submission; the remaining unmapped part of this branch is the later `AAS_PredictClientMovement` hazard/blockage rejection. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1000-L1042】【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1135-L1199】【F:src/botlib/ai_move/bot_move.c†L3200-L3273】【F:src/botlib/ai_move/bot_move.c†L3393-L3487】【F:tests/ai/test_bot_move.c†L591-L707】

## Walk Travel Reconstruction (2026-05-26)
* Q3's active `BotTravel_Walk` targets `reach->start` first, runs the blocked probe against that approach direction, switches to `reach->end` when the bot is within ten units of the start, crouches near destination areas that do not allow `PRESENCE_NORMAL`, and scales speed with `BotGapDistance`. The reconstruction now follows that ordering instead of moving straight to the reachability end at bridge max-walk speed. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1345-L1393】【F:src/botlib/ai_move/bot_move.c†L1863-L1925】
* Area presence checks now read the AAS area settings through `BotMove_AreaPresenceType`, treating unset reconstructed test areas as normal-presence areas. Focused tests cover reach-start steering plus gap slowdown, and near-start end steering plus crouch-only target areas. 【F:src/botlib/ai_move/bot_move.c†L668-L689】【F:tests/ai/test_bot_move.c†L711-L875】

## Crouch And Barrier Travel Reconstruction (2026-05-26)
* Q3's active `BotTravel_Crouch` ignores the configurable crouch velocity and submits `EA_Crouch` plus a fixed 400-unit move toward `reach->end` after the shared blocked probe. The reconstruction now uses that retail constant and end-directed steering instead of the bridge max-crouch velocity path. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1438-L1461】【F:src/botlib/ai_move/bot_move.c†L1927-L1950】
* Q3's active `BotTravel_BarrierJump` steers toward `reach->start`, caps the approach distance at 60 units, moves at `6 * distance`, and only submits `EA_Jump` plus `MFL_BARRIERJUMP` / `jumpreach` when the start is closer than nine units. The reconstruction now preserves that approach/jump split rather than jumping immediately toward `reach->end`. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1468-L1496】【F:src/botlib/ai_move/bot_move.c†L1954-L1992】
* Focused route-level coverage pins the crouch action/speed, barrier approach speed, and close-range jump state changes through `BotMoveToGoal`, so the active dispatch wiring for travel types 3 and 4 is tested through the same reachability path used by retail. 【F:tests/ai/test_bot_move.c†L880-L1106】

## Walk-Off-Ledge Travel Reconstruction (2026-05-26)
* Gladiator's `sub_100327f0` preserves the active walk-off-ledge helper but diverges from stock Q3 in the near-edge constants: the binary uses a 60-unit near threshold, a 15-unit horizontal reach threshold, a 200 fallback when the horizontal fall velocity cannot be solved, and `380 - (300 - 2 * dist)` for short near-vertical edges. The C helper now follows those HLIL constants while still steering toward the reachability endpoint and submitting the shared blocked probes. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L39554-L39620】【F:src/botlib/ai_move/bot_move.c†L2037-L2096】
* `BotMove_HorizontalVelocityForJump` reconstructs the Q3 jump/fall velocity formula locally from the AAS helper, using the bridge gravity and maxvelocity libvars because the standalone AAS movement helper is not yet lifted in this tree. Focused coverage pins the 400-speed far path, the computed fall-speed path, and the Gladiator short-edge formula through active `TRAVEL_WALKOFFLEDGE` dispatch. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_aas_move.c†L1062-L1101】【F:src/botlib/ai_move/bot_move.c†L587-L640】【F:tests/ai/test_bot_move.c†L1107-L1334】

## Ladder Swim Water-Jump Teleport Reconstruction (2026-05-26)
* Active ladder travel now mirrors Q3 and Gladiator's `sub_10032fc0`: compute the direction to `reach->end`, bias the view direction vertically with `3 * dir[2]`, submit a zero-speed `EA_Move`, then use the movement-action `EA_MoveForward` instead of a direct velocity move. The EA layer now exposes `EA_MoveForward` and `EA_MoveUp` so these helpers can use the same action bits as the retail elementary actions. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1995-L2044】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L39847-L39869】【F:src/botlib/ai_move/bot_move.c†L1999-L2035】【F:src/botlib/ea/ea_main.c†L535-L553】
* Active swim travel now steers to `reach->start`, performs the shared blocked probe, sets swim-view, and moves at the fixed retail 400 speed instead of the bridge max-swim velocity. Active water-jump now follows the Q3 action path: perturb the vertical aim by `15 + crandom() * 40`, submit `EA_MoveForward`, and add `EA_MoveUp` when the horizontal distance to the exit is under 40 units. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1532-L1585】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L39425-L39490】【F:src/botlib/ai_move/bot_move.c†L2109-L2170】
* Active teleport travel now does real pre-teleport approach movement: it returns no movement once `MFL_TELEPORTED` is set, otherwise walks to `reach->start`, preserves vertical steering only while swimming, probes blockage, slows to 200 inside 30 units, and marks swim-view for swimming approaches. Focused tests exercise all four helpers through `BotMoveToGoal` and pin the new EA action wiring. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L2051-L2075】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L39880-L39921】【F:src/botlib/ai_move/bot_move.c†L2173-L2212】【F:tests/ai/test_bot_move.c†L1331-L1628】

## Jump Travel Reconstruction (2026-05-26)
* Active `TRAVEL_JUMP` now mirrors the Q3/Gladiator run-start state machine instead of immediately jumping toward `reach->end`: derive a run-up direction behind `reach->start`, shorten the 80-unit run-start when the sampled area leaves `ms->reachareanum`, run toward that point at `5 * min(dist, 80)`, and only launch once the bot is between the run-start and jump-start windows. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L1889-L1953】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L39658-L39780】【F:src/botlib/ai_move/bot_move.c†L2095-L2229】
* The launch branch now preserves the retail action timing: `EA_Jump` inside 24 units of `reach->start`, `EA_DelayedJump` inside 32 units, `EA_Move` toward `reach->end` at speed 600, and `jumpreach` set once the launch action is active. Focused coverage pins run-up, immediate jump, and delayed jump dispatch through `BotMoveToGoal`. Full parity for this family still needs the standalone `AAS_JumpReachRunStart` / `AAS_PredictClientMovement` lift and the separate weapon-jump helpers. 【F:tests/ai/test_bot_move.c†L1626-L1867】

## Airborne Finish Reconstruction (2026-05-25)
* Q3 splits `BotMoveToGoal` into a normal route-selection branch for bots on ground, swimming, or against ladders, and a finish branch for bots with no movement surface. The finish branch reuses `lastreachnum` and dispatches to `BotFinishTravel_*` helpers instead of selecting a fresh reachability midair. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L3400-L3481】【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40744-L40790】
* The C reconstruction now preserves that split with `BotMove_HasMovementSurface`, `BotMove_ShouldKeepReachability`, and `BotMove_DispatchFinishTravel`. Finish handlers cover barrier jump, walk-off-ledge, jump, water jump, elevator/func_bobbing carry, jump pad, and weapon-jump air control, while grapple continues through the existing grapple state machine. 【F:src/botlib/ai_move/bot_move.c†L789-L806】【F:src/botlib/ai_move/bot_move.c†L2220-L2419】【F:src/botlib/ai_move/bot_move.c†L2713-L2780】
* The route-selection branch now validates whether an existing `lastreachnum` can remain active before calling `BotGetReachabilityToGoal`, matching Q3's timeout, goal-change, area-change, elevator/func_bobbing, and grapple reset checks. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L3224-L3311】【F:src/botlib/ai_move/bot_move.c†L2435-L2487】【F:src/botlib/ai_move/bot_move.c†L2944-L3067】

## View/Predict Route Query Reconstruction (2026-05-25)
* Q3's `BotMovementViewTarget` does not call routing from the live move-state origin after the first hop. It rebuilds a temporary route context from `reach.end`, the reached area, `ms->lastgoalareanum`, and the locally tracked previous area, then explicitly passes no avoid spots while preserving avoid-reach state. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L843-L891】
* `BotPredictVisiblePosition` uses the same synthetic route context pattern with local `lastgoalareanum` and `lastareanum` values while walking up to twenty reachabilities. This keeps visible-position prediction from drifting into immediate backtracking when the helper is not attached to a live movement state. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L912-L968】
* The reconstruction now centralizes that setup in `BotMove_GetReachabilityFromPoint`, used by both helpers. `BotMovementViewTarget` ignores live avoid spots for lookahead, and the focused test covers the case where movement avoidance would otherwise hide the next look target. 【F:src/botlib/ai_move/bot_move.c†L1796-L1840】【F:src/botlib/ai_move/bot_move.c†L3533-L3657】【F:src/botlib/ai_move/bot_move.c†L3660-L3718】【F:tests/ai/test_bot_move.c†L1484-L1573】

## Reachability Area Probe Reconstruction (2026-05-25)
* Q3's `BotReachabilityArea` always starts with `PRESENCE_CROUCH` bounds, traces three units down with the calling client as the pass entity, returns the fuzzy area immediately when standing on world, maps func_plat/func_bobbing hits through the model reachability, and only falls 800 units when standing on some other entity outside a reachable/swimmable area. 【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L301-L359】
* The reconstruction now mirrors that exported helper through `BotMove_CrouchPresenceBounds` and the Q2 bridge trace: world hits skip the non-mover fallback, non-mover fallback traces down with no pass entity, and both probes use the retail crouch box. 【F:src/botlib/ai_move/bot_move.c†L999-L1017】【F:src/botlib/ai_move/bot_move.c†L3453-L3528】
* Focused coverage captures the trace contract directly, including the `{-15,-15,-24}` / `{15,15,8}` bounds, client pass entity on the short probe, and `-1` pass entity on the fall-down probe. 【F:tests/ai/test_bot_move.c†L1396-L1481】

## Notable Edge Cases to Preserve
* **Accidental Platform Landings:** HLIL validates mover model numbers and logs when an elevator/bobbing platform lacks a reachability, implying the need to replicate Quake’s fallback that marks the bot as blocked in that scenario. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L18023-L18210】【F:dev_tools/Quake-III-Arena-master/code/botlib/be_ai_move.c†L3092-L3176】
* **Grapple Slack Management:** The hook is disengaged if the reel distance shrinks below 0.4 seconds of travel time or grows by more than a unit, ensuring the bot does not remain tethered to dead geometry. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40405-L40460】
* **Travel Type 0xE Gating:** Before accepting a grapple reachability (type `0xE`), Gladiator checks the remaining timeout and whether bit `0x80` is already set in the move flags, mirroring Quake’s rule that avoids re-firing the hook too soon. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40638-L40655】
* **Unsupported Successor Travel Types:** Travel types 15-17 (`TRAVEL_DOUBLEJUMP`, `TRAVEL_RAMPJUMP`, and `TRAVEL_STRAFEJUMP`) remain intentionally unimplemented in the retail Gladiator dispatch path. The reconstructed mover therefore preserves the warning fallback instead of routing these Quake III successor reachabilities through an approximate handler.
* **Mover Relink Diagnostics:** Gladiator copies the move result returned by each travel helper, so diagnostics accumulated immediately before dispatch must be merged back afterward. This keeps the "without reachability" mover relink path observable while retaining the original helper-copy structure.
* **Reachability Resets on Goal Change:** When `arg2[0x10]` equals the goal’s area, the HLIL returns immediately; otherwise it frees the old reachability handle (`j_sub_10011040`) and re-queries via `j_sub_100310e0`, just like Quake’s `BotMoveToGoal` re-pathing after area changes. 【F:dev_tools/gladiator.dll.bndb_hlil.txt†L40791-L40820】

These notes should guide the future C translation so Gladiator’s movement logic retains the quirks captured by the original binary.
