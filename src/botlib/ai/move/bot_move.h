#pragma once

/*
 * TODO: Expose movement prediction helpers analogous to Quake III's be_ai_move.c.
 * Expected API surface includes BotAllocMoveState, BotFreeMoveState, BotInitMoveState,
 * BotResetMoveState, BotMoveToGoal, BotMoveInDirection, and reachability queries so
 * the interface export table can issue navigation updates every frame.
 */
