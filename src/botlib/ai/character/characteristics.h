#pragma once

/*
 * Gladiator bot characteristic index helpers. The project ships the original
 * Quake II character definitions in dev_tools/assets/chars.h; however those
 * values live outside the build include path.  This header provides the subset
 * required by the modernised bot interface.
 */

#define CHARACTERISTIC_REACTIONTIME 11
#define CHARACTERISTIC_EASY_FRAGGER 42
#define CHARACTERISTIC_ALERTNESS 43
